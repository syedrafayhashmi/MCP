{
  "name": "mcp-issue-tracker-root",
  "version": "1.0.0",
  "description": "Root package for MCP Issue Tracker deployment",
  "scripts": {
    "build": "cd mcp-issue-tracker/backend && NPM_CONFIG_PRODUCTION=false npm install && npm run build && cd ../frontend && NPM_CONFIG_PRODUCTION=false npm install && npm run build && cp -r dist ../backend/public"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}